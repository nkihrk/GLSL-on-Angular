import { PointerDirective } from './pointer.directive';

describe('PointerDirective', () => {
  it('should create an instance', () => {
    const directive = new PointerDirective();
    expect(directive).toBeTruthy();
  });
});
